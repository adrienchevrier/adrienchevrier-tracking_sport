import garminService from './services/garminService';


const ValidationService = {
    tot_hours: function(value) {
        //inspect the value
    },

    secondValidationMethod: function(value) {
        //inspect the value
    }
};
